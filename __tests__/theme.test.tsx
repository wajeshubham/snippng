import { render, waitFor } from "@testing-library/react";
import Home from "../pages/index";

describe("Theming", () => {
  it("renders with dark mode default", async () => {});

  it("toggles the theme", async () => {});
});
